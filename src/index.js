import React from "react";
import ReactDOM from "react-dom";

import OnlyDiagramBpmn from "./OnlyDiagramBpmn";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <OnlyDiagramBpmn />
  </React.StrictMode>,
  rootElement
);
